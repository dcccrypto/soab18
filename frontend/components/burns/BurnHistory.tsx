import { Table } from '@/components/ui/table'
import { formatNumber, formatDate } from '@/lib/utils'
import { BURN_INFO } from '@/constants'

export const BurnHistory = ({ burnHistory }) => {
  return (
    <Table>
      {/* ... table implementation ... */}
    </Table>
  )
} 